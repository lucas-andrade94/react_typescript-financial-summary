import { HeaderContainer, HeaderText } from "./styles";

const Header = () => {
  return (
    <HeaderContainer>
      <div className="container">
        <HeaderText>Financial Summary</HeaderText>
      </div>
    </HeaderContainer>
  );
};

export default Header;
